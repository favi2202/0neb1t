1. При XOR двух одинаковых последовательностей результат состоит только из нулевых битов: X XOR X = 0. Строка «Привет, мир!» не является ASCII; в UTF-8 её одинаковые байты всё равно дадут нули.
2. 1010 XOR 0000 = 1010. Результат равен первой исходной строке.
3. (A XOR B) XOR A = B.
